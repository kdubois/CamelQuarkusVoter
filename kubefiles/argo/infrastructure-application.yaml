apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: camelquarkusvoter-infrastructure
  namespace: openshift-gitops
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: cameldemo
  project: default
  source:
    path: kubefiles/base/infrastructure
    repoURL: https://github.com/kdubois/camelquarkusvoter.git
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

# Made with Bob
